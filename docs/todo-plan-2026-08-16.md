# TODO 待办编排实施方案(2026-08-16,持久化)

> 目的:TODO.md 剩余 15 条待办的系统性编排,供本会话(及后续会话)照单施工,
> 防止长上下文注意力涣散。**进度以本文档勾选为准**,完成后回填 TODO.md。
> 原则:子 agent 并行按「文件所有权互斥」分波;主线只做质量门(tsc/bun test/build/e2e)
> 与冲突裁决;零兜底原则贯穿(CLAUDE.md)。

## 剩余待办盘点(来源 TODO.md L23-36 + ui-improvements 遗留)

| 编号 | 内容 | 主要落点文件 |
|---|---|---|
| 23a | S2 编辑器/大厅 window.prompt/alert → 卷轴对话框 | LobbyScreen / EditorScreen |
| 23b | S8 SoloSetup 国号非法 → 红边 + 内联即时提示 | SoloSetupScreen |
| 24 | 地图边缘与外部割裂 → 视觉连续(晕影/延展地形) | board/StaticLayers 或 TerrainLayer |
| 25 | 城池视觉放大(太小看不清) | board/Tile.tsx(旗/匾/印尺寸) |
| 26 | 行军音效换浅/轻快(频繁触发不能吵) | audio 资源 + AudioProvider |
| 27 | 选都阶段点击不可选城池零响应 → 交互反馈 | GameScreen + fx |
| 33 | 所有城池(含特殊地点)单击可查看详情 | GameScreen 相位路由 |
| 34 | 详情卷轴去掉右上 X,点空白关闭 | 城池详情卷轴组件 |
| 35 | 选都灰化不可选城;可选项首击=看详情,详情内确认/取消定都 | GameScreen + Tile state + 详情卷轴 |
| 28 | 首页预设国号持久化;重名加方位前缀(东西南北前后大+1 凑 8) | core + SoloSetupScreen + online 房间 |
| 29 | 最大玩家数扩展到 8 人 | core/theme 色板 + engine 上限 + UI 座位 |
| 30 | 棋子旗子不明显/行军看不清 → 放大+强化 | board/TokenLayer |
| 31 | 地产数据修正:只有 3 级;无过路费/升级费(到达免费升级) | maps json + core 规则 + 详情展示 |
| 32 | 手牌区按钮大小/样式七扭八歪 → 系统性重设计 | HandPanel |
| 36 | 珍宝/英雄详情预留图片位,配三国杀武将画像(比例一致) | CardDetailScroll + public/assets |

## 波次编排(文件所有权互斥 → 可并行)

### Wave 1 · 引擎与数据(先行,单独 1 agent,串行)
**条目:#31 → #29 → #28(核心逻辑部分)**
- #31 先做:核实 maps/*.json 的等级/费用字段与核心规则——删过路费/升级费概念、
  统一 3 级;同步城池详情展示文案;补/改单测(零兜底:规则错了要炸测试)。
- #29 在 #31 后:玩家上限 2-4 → 2-8(色板 8 色、engine 校验、SoloSetup 人数选择、
  联机房间座位数)。#28 的 8 个前缀正是为 8 人同名服务,顺序耦合。
- #28 逻辑半:国号预设存 localStorage + 联机重名前缀分配(东西南北前后大小,8 个;
  房间侧分配前缀并回写显示名)。UI 半(设置入口)归 Wave 4。
- 交付门:tsc / bun test 全绿(本波不动 e2e 页面结构,e2e 留总验)。

### Wave 2 · 棋盘视觉(1 agent,独占 board/ 目录)
**条目:#24 + #25 + #30**
- 同属 BoardView 渲染体系(StaticLayers/Tile/TokenLayer),一个 agent 做保证视觉口径
  一致(放大比例、配色不互相打架)。
- #24:边缘过渡(如地图外围晕影渐隐/纹理延展,不再硬切背景)。
- #25:城池整体 scale 放大(旗、匾、价格签联动),总览 viewBox 间距已留 OVER。
- #30:旗子放大 + 行军中棋子强调(金边/拖影/落脚指示环),移动时可辨。
- 交付门:tsc + scrolls e2e(棋盘相关)+ 截图肉眼验收(主线)。

### Wave 3 · 城池交互流(1 agent,独占 GameScreen + 详情卷轴)
**条目:#27 + #33 + #34 + #35(四条是一条交互链,必须同一人做)**
- #35 重构选都交互:不可选城灰化 → 可选城首击=详情 → 详情内「定都于此/再看看」。
  #27 的反馈(点灰城 toast/抖动)是 #35 的子集。
- #33:Playing 相位下特殊地点(Chance/驿站等)也可点看详情(路由放开)。
- #34:详情卷轴删 X,点空白/遮罩关闭(保留 Esc)。
- 交付门:tsc + solo e2e(选都流程变了,需同步改 spec 断言)+ 主线手测。

### Wave 4 · 表单与弹窗收尾(1 agent,独占两个 Screen + home)
**条目:23a S2 + 23b S8 + #28(UI 半)**
- S2:LobbyScreen/EditorScreen 的 prompt/alert → 复用/新建卷轴 ConfirmDialog + 输入卷轴。
- S8:SoloSetup 国号非法即时红边 + 内联 xs 提示(替代常驻灰字)。
- #28 UI:首页(或 SoloSetup)提供「我的国号」预设输入,持久化,默认带入。
- 交付门:tsc + setup/online e2e。

### Wave 5 · 手牌区重设计(1 agent,独占 HandPanel + game-hud.css)
**条目:#32**
- 按钮尺寸/圆角/字号统一规范(对齐 ui-improvements W3 三档字号阶梯),
  主行动/次级/信息三级按钮体系;顺手与 Wave 3 的详情按钮口径对齐(读其产出)。
- 交付门:tsc + scrolls e2e + 截图验收。

### Wave 6 · 音效(1 agent,独占 audio 资源与触发点)
**条目:#26**
- 行军音效替换:短促轻快(≤150ms,低音量,可用 WebAudio 合成或新采样);
  频繁触发不叠加吵(同音去重/限流)。
- 交付门:tsc + 手测听感(主线)。

### Wave 7 · 珍宝/英雄画像(单独 1 agent,或延后独立会话)
**条目:#36**
- CardDetailScroll 预留统一比例图片位;联网获取三国杀武将画像(用户已授权学习用途、
  不商用),统一裁切比例入库 public/assets/heroes/;珍宝可先用占位纹样。
- 风险:外网资源获取不稳定、体积大——独立会话做,不阻塞其余条目。

### 并行度
- Wave 1 先行单独跑(引擎是其余波的依赖:详情字段、8 人、前缀)。
- Wave 1 完成后:**Wave 2 ∥ 3 ∥ 4 ∥ 5 ∥ 6** 五路并行(文件所有权互斥,见上表)。
- Wave 7 延后。
- 每波结束主线统一跑质量门:tsc → bun test → build → 全量 e2e;
  并行期间共享文件冲突以「波所属」裁决,主线不得代改他人文件。

## 进度勾选

- [x] Wave 1 · #31/#29/#28核心(引擎与数据)— aa1701f
- [x] Wave 2 · #24/#25/#30(棋盘视觉)— 0c87af0
- [x] Wave 3 · #27/#33/#34/#35(城池交互流)— 8a4edd6
- [x] Wave 4 · S2/S8/#28UI(表单与弹窗;S2/S8 核实已随 Wave 1 完成)— 0bd6261
- [x] Wave 5 · #32(手牌区重设计)— 3e5448c
- [x] Wave 6 · #26(行军音效)— 8583016
- [x] Wave 7 · #36(画像资源)— 5a50d08(3 英雄三国杀立绘 596KB + 珍宝纹样 + 详情画像位)
- [x] 总验收 + TODO.md 回填 — tsc 0 错 / bun test 177 绿 / build 过 / e2e 37/37 绿

## 第 8 个前缀决议(预定,待用户否决)

东西南北前后大 + **小**(小宁)。备选:仲/少。
