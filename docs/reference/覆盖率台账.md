# 覆盖率台账

低频体检记录,由 `coverage-audit` skill(`.agents/skills/coverage-audit/`)维护。目的:**守住基线,不是刷数字**。每次体检与上一条对照,只登记值得行动的发现。

口径:`bun test --coverage`(bun 内建);体检重点只有 `src/core/`;app/ UI 层由 e2e 兜,覆盖率无诊断价值。

## 2026-09-13(基线)

- bun 1.4.0,465 例全绿(2689 断言),耗时约 8s。
- **core/:除 theme.ts 外全部 ≥88.9% 行覆盖**,多数 100%。theme.ts 行 25%/分支 97.7%——数据表文件,正常。
- game.ts 行 96.4% / 分支 99.1%,未覆盖仅 400-412(国号占用冲突)、570-572(跳过选都)两小段。
- 薄弱点(encounters.ts 行 88.9%,未覆盖 155-167、210-211):机遇配置的非法值回退分支。低优先级——零兜底哲学下该分支本就"不该到达"。
- 其余:scripts/room.ts 97%;engine-helpers / fetch-asset 低(CLI/网络工具,按需评估);app/fx 大量 0%(正常态,见 skill 判读口径)。
- **结论:无需行动的缺口,基线即此。** 后续体检对照本条,只盯掉落。
