# 死代码台账(knip 体检记录)

> 刻意低频的体检账本:每跑一次 [dependency-audit skill](../../.agents/skills/dependency-audit/SKILL.md) 记一节,与上一条对照。哲学同[覆盖率台账](./覆盖率台账.md)——守基线、清已知,不追零;触发时机与判读口径见 skill,不进 CI、不设阈值。

## 2026-09-26 首跑(knip 引入日)

配置状态:入口 = index.html + 5 个手工脚本(cli/replay-log/fetch-asset/make-chessboard-map/shot);忽略 = `public/fonts/**`(静态资源)与 `src/app/components/ui/**`(shadcn 底件,CLI 管理,导出即 API 面,清理会制造与上游的 diff 噪声)。死依赖 sharp、@tauri-apps/api 已于引入日删除(pngjs/@types/pngjs 系 fetch-asset.ts 在用,knip 配置修正后消失)。

**基线:未用导出 33 + 未用导出类型 34,死文件 0,死依赖 0。**

分类判读(下次体检对照这里,消失的划掉,新增的问一句为什么):

- **scripts/ 库层(7 导出 + 4 类型)**:`engine-helpers.ts` 的 promptFor/botOwnsDecision/autoResolveBots、`room.ts` 的 AUTOPILOT_SLOW_MS/AUTO_ROLL_DELAY_MS/statusOf、`room-persistence.ts` 的 engineFromRecord 等——CLI/服务器共享层的候补接口,倾向**真死代码**,顺手工单删。
- **app/ 桶文件(scroll/index.ts 等,~15 项)**:barrel 再导出与 Props 类型无人走桶消费——要么删桶、要么让消费方走桶,二选一,**随下次动到该屏时顺手清**。
- **app/ 散件(~12 项)**:tokenRenderPos、FIT_VIEW、SynthAudioPlayer(音频合成备胎)、showTurnBanner、resetFxOrchestration、useMediaQuery 族、useActivePlayer、playerNetWorth、myAutoPilotOn 等——多为改版遗留(如 #253 三区骨架后),倾向真死,**逐个核实后删**。
- **core/(8 导出)**:heroSkillIntent、demolishTargetOk、jinnangTargetOk、skillCooldownLeft( choices 内部件,收私有即可)、TAP_MAX_MOVE、mulberry32、FATE_EVENTS、GUOHAO_PREFIXES、hex(theme)、MOMENTS(timing)——core 是门禁重点,**导出收缩不改行为,优先清**;但逐个确认非 #234 等在途工单的预留面。
- **未用导出类型(34)**:多为组件 Props(interface 导出仅惯例),价值低,**随桶文件一并处理**,不单独立票。

处置原则:体检只记录与分类;真正删除走独立小工单(动 core 的每处都过 `bun run check:core` + 全量单测)。下次体检的合格线 = 基线无新增 + 已删项划账,不要求清零。
