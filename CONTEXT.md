# dafung-web 联机对局

权威引擎 + 瘦客户端的三国大富翁。两种对局形态:**单机模式**(1 真人对阵电脑)与**联机模式**(每人一设备、经中心云服务器同步)。早期为本地热座(单设备多真人轮流),已移除。

## 语言

**单机模式(Solo Mode)**:
开局设置屏「起兵」进入的本地对局:恰好 1 位真人,其余诸侯全部由电脑执掌。设置屏的座位表与联机大厅保持同构(类型列锁死:首行为真人,其余为电脑),使单机成为联机形态的先行试验场。
_Avoid_: 热座 / 单机热座(多真人同屏轮流已移除)、本地多人

**Authority(权威引擎)**:
唯一受信任的对局裁决者。所有掷骰、落格、胜负只在它内部发生;客户端从不自行裁决。
_Avoid_: server(过载——既指部署主机,又指 HTTP 进程)、host(与"房主"混淆)

**Room(房间)**:
一局进行中的对局。有独立的玩家、回合状态与珍宝牌堆;多局 Room 之间互不影响。
_Avoid_: game(与"一整局对局"混淆)、session(网络语境指一次登录连接)

**Lobby(大厅)**:
Room 开局前的等待态——在引擎 Setup 之前。玩家在此加入并占 Seat;Host 开局后构造引擎、进入 Setup → Playing。
_Avoid_: waiting room

**Seat(座位)**:
Room 里的一个玩家位(共 2–4 个)。轮到谁即"谁的 Seat 活跃"。
_Avoid_: player(既指真人又指数据结构)、slot

**Host(房主)**:
Room 的创建者(Seat 0),掌握开局前管理权(选座位数、开局);对局中可解散房间、强令 bot 接管掉线 Seat。
_Avoid_: owner(与城池持有者混淆)、admin

**Client(客户端)**:
玩家设备上的瘦端(浏览器 / Android)。只提交命令、接收并渲染状态,不持有权威裁决。
_Avoid_: frontend(泛指渲染层)、app

## 地图

**选择地图(地图菜单)**:
开局设置屏的二级屏入口(按钮文字「选择地图」)。点进去是从多张备选地图中挑一张作为本局使用的唯一途径。内置地图与玩家自建地图在此统一呈现。
_Avoid_: 择图 / 沙盘 / 疆域(古风化命名已弃用,统一用直白表述)

**内置地图(Built-in Map)**:
随版本打包发布、位于 `public/maps/` 的地图 JSON。玩家不可修改。如 `sanguo.json`。
_Avoid_: 官方地图、默认地图

**自建地图(Custom Map)**:
玩家用编辑器创建或从文件导入、存储在浏览器 `localStorage` 的地图。在「选择地图」里与内置地图并列可选。
_Avoid_: 自定义地图(语义模糊)、编辑器地图(工具名而非产物名)

## 表现

**表现事件(PresentationEvent)**:
一次引擎推进所产生的可播放信号(掷骰/棋子位移/金额变动/印章/横幅/音效)。单机与联机各自「提取」事件,统一交给唯一播放器按序呈现。见 ADR-0010。
_Avoid_: 动画事件(过窄——含音效)、特效(语义模糊)

**FxSink**:
表现播放的四个能力(audio/骰子/浮层/棋子行军)收口成的窄接口。生产环境组装真实单例,测试用内存 sink 录制断言。
_Avoid_: 渲染后端、特效引擎

## 结构(客户端)

**驱动仲裁(Drive Arbiter)**:
"谁有资格推进本地引擎"的单点裁决(FIFO 互斥会话)。单机四路驱动(人类步/bot 接棒/托管/开局接棒)均经它排队,取代散落的 busy 布尔。见 ADR-0011。
_Avoid_: busy 锁(实现细节)、调度器(过泛)

**表现态视图(engine.presentation)**:
引擎表现信息(lastRoll/lastMove/lastTransaction/drainFloaters)的唯一只读入口,把"谁写、谁读、何时失效"的不变量收进接口。写通道仅 applyPresentationMove。
_Avoid_: 瞬时字段(语义含糊)

## 交互

**抉择卷轴(Decision Scroll)**:
需要玩家权衡的多选操作(购地/扩军/择路/招贤/交涉/破产)的唯一入口:轮到即自动弹出的卷轴弹窗,不可误关。侧栏手牌区只放信息与「行军」主行动,不放决策按钮。见 ADR-0012。(经过都城必停已无玩家抉择,不占卷轴。)
_Avoid_: 决策按钮/内嵌决策(已废,勿再往侧栏加选择按钮)、确认框(语义过轻)

**选项集(Choice-set)**:
某决策相位下决策者当前全部可用行为的清单(每项带 available/reason),由引擎的集中注册表实时计算。选项集 ≤1 时引擎直接自动执行默认行为(浮字轻提示+战报),不弹卷轴——弹卷轴 ⇔ 至少两个真实选项。破产清算例外(重大不可逆,仍弹确认)。见 ADR-0013。
_Avoid_: options(过泛)、actions(与 GameCommand 混淆)

**默认行为(Default action)**:
选项集 ≤1 时引擎自动执行的那一个行为(如放弃购买)。
_Avoid_: fallback choice(不是兜底,是产品决策)

**对局日志(Game Log)**:
一局一个 jsonl 文件(联机=服务器 logs/,单机=IndexedDB),局头(地图/seed/座位)+中文事件流+机读 params+玩家命令流双层,支持命令流重放复现。30 天保底清理。见 ADR-0014。
_Avoid_: 战报(已退役 UI 概念)、history

**命令流重放(Command Replay)**:
以 seed+座位+玩家命令序列重建确定性引擎的复现方式。_Avoid_: 读叙述推理(精度不足)

## 声望与机遇

**声望(Reputation)**:
每位 Seat 的对局内数值(-100~+100,开局恒 0,预留非 0 配置口子),决定机遇抽取时好运/中性/霉运三档的真实比例;只被机遇内的抉择与结果改变,不随回合自然波动。
_Avoid_: 运气(口语,指代不明)、福缘/威望(弃用)

**机遇(Encounter)**:
行军落格后按概率触发、早于城池结算的事件:带 好运/中性/霉运 之一的档位、内容标签(武将/银两/城池…),效果多数面向系统侧、少数引发玩家间互动;带抉择的机遇走抉择卷轴,由玩家选择是否承担代价(如「携民渡江」:舍银两换声望)。id 用中文且必须自带因果(名字与效果互证)。
_Avoid_: 事件(已被表现事件/对局日志占用)、奇遇/际遇(弃用)

**体力(Stamina)**:
每位 Seat 的对局内数值(0~100,开局 100),常规对局不变;机遇与武将技能使其增减。耗竭(归 0)时立即受罚——降 1 级房产(全部 0 级则失去一座房产)并跳过下一回合,随后重置为 100。
_Avoid_: 血量/HP(非战斗伤害概念)、精力(歧义)

**档位(Fortune Tag)**:
机遇的三分定性——好运(纯正收益)/中性(有得有失需权衡)/霉运(纯负收益)。三档基准概率可配置,配置值之和不必为 100(按配置值占比归一为真实概率)。
_Avoid_: 稀有度(那是珍宝 level)、品级

## 锦囊与暗牌

**锦囊(Jinnang)**:
暗置手牌卡牌系统,全游戏唯一的隐藏信息,博弈乐趣的核心,对局基本机制(无开关)。经锦囊格/机遇奖励/声望献计/军师来投/起手发牌获取后暗置于己方,只在「自己回合开始、掷骰前」的锦囊卷轴中主动使用;每张牌的目标域(自身/指定一名玩家/指定两名玩家/全体其他玩家)由牌面固定。id 用中文且自带因果(名字与效果互证)。
_Avoid_: 技能(那是武将被动触发)、宝物(与珍宝混淆)、事件(已被占用)、反应牌(非自己回合使用,当前不做)

**锦囊手牌**:
某座位暗置的锦囊集合。内容仅本人可见(联机经 per-seat 投影),数量全员可见(牌背计数,本身就是博弈信息)。上限 3 张,满手时抽牌作废。
_Avoid_: 手牌(裸用——易与侧栏手牌区混淆)、暗牌(口语)

**锦囊标签**:
锦囊的功能类别:谋(计与信息)/攻(劫夺与破坏)/守(保身)/援(壮己)。每回合每类限用一张;一张锦囊可有多枚标签,使用即同时占用其全部标签的本回合名额。
_Avoid_: 内容标签(机遇的内容分类——银两/武将/城池那套)、稀有度(珍宝 level)

**牌库(Jinnang Deck)**:
一局一副的有限锦囊牌堆:开局按目录构成生成、抽空即止(弃牌堆不回流),随快照可序列化;剩余牌数全员可数。
_Avoid_: 卡池(那是机遇目录的按权重独立抽取)、抽卡堆

**拼点**:
两名玩家各掷一枚骰子比大小的锦囊结算方式;平局则双方无事、该锦囊作废(自担风险)。
_Avoid_: 对决/斗点(口语化)、比大小(含糊)

**锦囊格**:
落格即从牌库抽一张锦囊的地图格(旧「机会格」语义复活重定义)。与机遇格区分:锦囊格必得一张,机遇格看概率触发。
_Avoid_: 机会格(旧名)、幸运格

**随机事件格**:
辅路格之一,落格触发随机小事件(原「锦囊随机事件」更名而来,机制不变,与锦囊系统无关)。
_Avoid_: 锦囊事件(与锦囊系统混淆)

